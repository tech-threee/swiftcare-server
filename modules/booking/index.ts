import { Router } from "express";

const router = Router()

// Make Bookings
router.post("/")

// Fetch my bookings
router.post("/")
router.post("/")
router.post("/")
router.post("/")
router.post("/")
router.post("/")

export default router